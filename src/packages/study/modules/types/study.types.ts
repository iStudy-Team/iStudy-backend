export enum AcademicYearStatusEnum {
    INACTIVE = 0,
    ACTIVE = 1,
    COMPLETED = 2,
}

export enum ClassStatusEnum {
    OPEN = 0,
    CLOSED = 1,
    COMPLETED = 2,
}

export enum ClassTeacherRoleEnum {
    MAIN_TEACHER = 0,
    ASSISTANT_TEACHER = 1,
    SUBSTITUTE_TEACHER = 2,
}

export enum ClassTeacherStatusEnum {
    INACTIVE = 0,
    ACTIVE = 1,
}
